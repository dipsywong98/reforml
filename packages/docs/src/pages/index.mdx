import { Box, Flex, Button } from "rebass";
import { Banner, NavGrid, Container, Grid, LogoGrid } from "..";
import Logo from "../components/Logo";

<Banner>
<Box mt='auto' />
<LogoGrid>
<Logo text='' size={192} />
<div>

# Reforml

## Simple yet flexible react form defined by yaml

<Flex alignItems='baseline' sx={{flexWrap: 'wrap'}}>
<Box mr={4} mb={3}>
<Button as='a' href='/getting-started'>Getting Started</Button>
</Box>
<Box sx={{w:'100%', pre:{w:'100%'}}} mb={3}>

```sh
npm i reforml
```

</Box>
<Box/>
</Flex>
</div>
</LogoGrid>
<Box my='auto' sx={{minHeight: '16px'}}/>
<Box>

```jsx live=true
() => {
  const [value, setValue] = useState({})
  const fields = useMemo(() => jsyaml.load(`
  myField:
    type: text
    label: my first reforml field
  `), [])
  return (
    <div>
      <BaseForm fields={fields} value={value} onChange={setValue}/>
      {value.myField}
    </div>
  )
}
```
</Box>
</Banner>

<!--
<Container py={5}>
</Container>
-->

export const title = 'Fields'

# Fields

Field components maps a field configuration to display, and Reforml provides some default field components for your ease of use,
and you can always change to use your own components or even add new field definitions (see [custom](./fields/custom)).
